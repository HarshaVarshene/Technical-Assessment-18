import { Routes } from '@angular/router';
import { TaskComponent } from './components/task.component'; // or './components/task'

export const routes: Routes = [
  { path: '', component: TaskComponent }
];

